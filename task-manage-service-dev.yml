server:
  port : 8083
  
spring:
  application:
    name: task-manage-service
  config:
    import: "optional:configserver:"
    
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
